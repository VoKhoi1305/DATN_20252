# Application
APP_PORT=3000
APP_ENV=development

# Database — Main (smtts_main)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smtts_main
DB_USER=smtts_user
DB_PASS=smtts_secret_2026

# JWT
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d
JWT_TEMP_EXPIRY=5m

# OTP
OTP_ISSUER=SMTTS

# Database — Biometric (smtts_biometric, separate per SBR-18)
BIO_DB_HOST=localhost
BIO_DB_PORT=5433
BIO_DB_NAME=smtts_biometric
BIO_DB_USER=smtts_bio_user
BIO_DB_PASS=change-in-production

# Face Recognition Service
FACE_SERVICE_URL=http://localhost:8000

# Throttle
THROTTLE_TTL=900000
THROTTLE_LIMIT=5
